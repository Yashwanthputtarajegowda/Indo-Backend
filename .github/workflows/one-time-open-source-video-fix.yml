name: One-time open-source video fix

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Source resolver is now loaded by the backend startup bootstrap."
